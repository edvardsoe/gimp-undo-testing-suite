name: Undo Test Integration Check

on:
  push:
  pull_request:

jobs:
  verify-undo-test-integration:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Verify undo test file exists
        run: test -f app/tests/test-undo.c

      - name: Verify undo test is registered in Meson
        run: grep -q "'undo'" app/tests/meson.build

      - name: Verify functional undo and redo test names exist
        run: |
          grep -q "undo-null-image" app/tests/test-undo.c
          grep -q "redo-null-image" app/tests/test-undo.c
          grep -q "test_add_layer_undo_redo" app/tests/test-undo.c
          grep -q "test_multiple_layer_undo_redo" app/tests/test-undo.c
          grep -q "test_redo_cleared_after_new_action" app/tests/test-undo.c

      - name: Print integration success message
        run: echo "Undo/redo test integration checks passed."   
